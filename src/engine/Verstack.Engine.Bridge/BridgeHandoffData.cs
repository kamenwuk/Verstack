namespace Verstack.Engine.Bridge;

/// <summary>
/// Базовый класс-маркер для данных (DTO), передаваемых между изолированными ECS-мирами (слоями).
/// Наследники этого класса (records) должны лежать в общих контрактах и содержать только данные,
/// необходимые принимающей стороне для инициализации игрока.
/// </summary>
public abstract record BridgeHandoffData;